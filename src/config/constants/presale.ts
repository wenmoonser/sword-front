import BigNumber from 'bignumber.js'
import { PresaleConfig } from './types'

const presale: PresaleConfig = {
    tokensLeft: null,
    tokensUnclaimed: null,
    userAllowance: null,
    busdBalance: null,
    cakeBalance: null
}

export default presale
